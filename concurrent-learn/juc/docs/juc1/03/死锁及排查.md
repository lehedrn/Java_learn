# 死锁及排查

## 什么是死锁

死锁是指两个或多个线程在执行过程中，因争夺资源而造成的一种**互相等待**的现象，若无外力作用，它们都将无法推进下去。

## 死锁产生的必要条件

1. **互斥条件**：一个资源每次只能被一个进程使用
2. **请求与保持条件**：一个进程因请求资源而阻塞时，对已获得的资源保持不放
3. **不可剥夺条件**：进程已获得的资源在未使用完之前不能强行剥夺
4. **循环等待条件**：若干进程之间形成一种头尾相接的循环等待资源关系

## 死锁示例代码
[DeadLockDemo.java](../../../src/main/java/com/coderlee/juc1/locks/DeadLockDemo.java)

## 死锁排查方法

### 1. 使用 jstack 工具

```bash
# 查看Java进程ID
jps

# 生成线程堆栈信息
jstack <pid>

# 或者直接输出到文件
jstack <pid> > thread_dump.txt
```


在输出中查找 "Found one Java-level deadlock" 字样来定位死锁。

### 2. 使用 jconsole 工具

- 启动 `jconsole`
- 连接到目标Java进程
- 切换到"Threads"标签页
- 点击"Detect Deadlock"按钮

### 3. 使用 VisualVM 工具

- 启动 VisualVM
- 连接到目标应用
- 在"Threads"选项卡中查看线程状态
- 使用"Thread Dump"功能分析死锁

## 避免死锁的方法

1. **避免嵌套锁**：尽量减少同时持有多个锁的情况
2. **按顺序加锁**：所有线程按照相同的顺序获取锁
3. **设置超时时间**：使用 `tryLock()` 方法设置超时
4. **使用并发工具类**：如 `ReentrantLock`、`Semaphore` 等
